# PromptRedundancyAssistant
An always-on, lightweight ComfyUI extension that watches prompt fields, detects unnecessary repetition, and offers contextual suggestions without adding a node to the workflow.  It stays out of the way until it finds something useful. There are no visible workflow nodes, API calls, telemetry, model downloads, or third-party runtime dependencies.
